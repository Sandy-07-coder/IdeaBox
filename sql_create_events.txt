-- Create the events table
CREATE TABLE public.events (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  title TEXT NOT NULL,
  description TEXT NOT NULL,
  event_date TIMESTAMP WITH TIME ZONE NOT NULL,
  location TEXT NOT NULL,
  registration_link TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Set up Row Level Security (RLS) so anyone can view events
ALTER TABLE public.events ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Enable view access for all users" 
  ON public.events FOR SELECT 
  USING (true);

-- Insert an upcoming event (14 days from now)
INSERT INTO public.events (title, description, event_date, location, registration_link)
VALUES (
  'Startup Pitch Night 2026', 
  'Join us for an evening of pitches from emerging startups. Meet founders, investors, and developers building the future.', 
  NOW() + INTERVAL '14 days', 
  'Innovation Hub, Main Auditorium',
  'https://example.com/register/pitch-night'
);

-- Insert a past event (7 days ago)
INSERT INTO public.events (title, description, event_date, location, registration_link)
VALUES (
  'Founder Networking Mixer', 
  'A casual meet and greet for founders, developers, and designers to connect, share ideas, and find potential co-founders.', 
  NOW() - INTERVAL '7 days', 
  'Central Cafe Downtown',
  'https://example.com/events/past/networking'
);
